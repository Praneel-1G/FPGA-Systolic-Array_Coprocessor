Current Progress: Need to Debug Uart
The RTL is fully simulated and synthesized using quartus on a DE10 standary cyclone V board,
but the the the physical UART is currently dropping the Bytes during the chunking process , 
so we are getting Stripped images , mostly due to computation of blank input matrices.

